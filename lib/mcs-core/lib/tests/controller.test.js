//const controller = new (require('../media/media-controller.js'));
//
//describe('media-controller', () => {
//  test('join', () => {
//    // join (roomId, type, params)
//    expect(controller.join(72013, 'SFU', {})).toEqual(expect.any(String));
//  });
//});
